CREATE TABLE no2Table (
  _id_x varchar ,
  lat float,
  long float,
  id_x int,
  nombre varchar,
  codigo varchar,
  redesid int,
  _id_y varchar,  
  date date,
  validoorig int,
  city varchar,
  estado varchar,
  valororig float,
  parametro varchar,
  estacionesid int,
  id_y varchar,
  hora int,
  fecha date
);

CREATE TABLE no2Table (
  _id_x varchar ,
  lat float,
  long float,
  id_x int,
  nombre_x varchar,
  codigo varchar,
  redesid int,
  _id_y varchar, 
  city varchar, 
  date date,
  estacionesid int,
  fecha date,
  hora int,
  id_y varchar,
  parametro varchar,
  estado varchar,
  validoorig int,
  valororig float,
  pronostico float
);

CREATE TABLE coPrediction (
  _id varchar,
  lat_x float,
  long_x float,
  id int,
  nombre_x varchar,
  codigo_x varchar,
  redesid_x int,
  _id_x varchar,
  lat_y float,
  long_y float, 
  id_x varchar,
  nombre_y varchar,
  codigo_y varchar,
  redesid_y int, 
  _id_y varchar,
  date date,
  validoorig int,
  city varchar,
  estado varchar,
  valororig float,
  parametro varchar,
  estacionesid int,
  id_y varchar,
  hora int,
  fecha date,
  pronostico float
);

CREATE TABLE coPrediction (
  _id varchar,
  lat_x float,
  long_x float,
  id int,
  nombre_x varchar,
  codigo_x varchar,
  redesid_x int,
  _id_x varchar,
  lat_y float,
  long_y float, 
  id_x varchar,
  nombre_y varchar,
  codigo_y varchar,
  redesid_y int, 
  _id_y varchar,
  city varchar,
  date date,
  estacionesid int,
  fecha date,
  hora int,
  id_y varchar,
  parametro varchar,
  estado varchar,
  validoorig int,
  valororig float,
  pronostico float
);


@app.route('/graphCO/<string:param>')
def graphco(param):
    table = param.lower()
    return render_template("graphs.html", param = param)

@app.route('/graphNO2/<string:param>')
def graphno(param):
    table = param.lower()
    return render_template("graphs.html", param = param)

@app.route('/graphO3/<string:param>')
def grapho(param):
    table = param.lower()
    return render_template("graphs.html", param = param)

@app.route('/graphPM2/<string:param>')
def graphpm2(param):
    table = param.lower()
    return render_template("graphs.html", param = param)

@app.route('/graphPM10/<string:param>')
def graphpm10(param):
    table = param.lower()
    return render_template("graphs.html", param = param)

@app.route('/graphSO2/<string:param>')
def graphso(param):
    table = param.lower()
    return render_template("graphs.html", param = param)